# 05단계 검수: WebSocket 실시간 대국, 핸드셰이크 인증 및 세션 동기화 (Phase 3)

## 검수 주체

- PM: 실시간 대국 흐름, 참가자 연결 감지 및 재접속 스냅샷 전송 요구사항 확인
- QM: WebSocket 프로토콜 이벤트 규격, 불법 수 거부(MOVE_REJECTED) 및 브로드캐스트 정합성 검증
- TL: WebFlux 기반 논블로킹 Reactive WebSocket 핸들러 구조, Sink 큐잉 및 메모리 안전성 확인
- SM: 비인가 사용자 접근 제어 및 연결 해제 시 안전한 세션 정리 검증

## 검수 범위

CP-05의 WebSocket 실시간 통신 및 세션 관리 계층을 검수한다.

- [x] WebFlux WebSocket 엔드포인트 구성 (`WebSocketConfig` - `/ws/games/**`)
- [x] 게임 세션 관리자 구현 (`GameSessionManager`)
  - [x] 대국별 세션 집합 스레드 안전 관리 (`ConcurrentHashMap`)
  - [x] Reactive Outbound Sink 기반 비동기 스트리밍
  - [x] 특정 대국 전체 브로드캐스트 및 개별 세션 단독 전송
- [x] WebSocket 프로토콜 핸들러 구현 (`ChessWebSocketHandler`)
  - [x] 연결 시 초기 `GAME_STATE_SNAPSHOT` 전송 및 `PLAYER_CONNECTION_CHANGED` 방송
  - [x] `PLAY_MOVE` 명령 수신 시 `GameService` 연동 및 `GAME_STATE_UPDATED` / `GAME_ENDED` 브로드캐스트
  - [x] 불법 수 및 예외 발생 시 `MOVE_REJECTED` 단독 회신
  - [x] `RESIGN` 명령 처리 및 `GAME_ENDED` 방송
  - [x] `SYNC_STATE` 명령 처리 및 최신 스냅샷 회신
  - [x] `OFFER_DRAW` 명령 처리 및 `DRAW_OFFERED` 방송
  - [x] 세션 종료 시 안전한 해제 및 오프라인 상태 방송
- [x] 단위 및 프로토콜 통합 테스트 (`ChessWebSocketHandlerTest`)

## 수행 결과

### 1. 주요 구현 산출물
- `com.jchess.api.dto.GameStateUpdatedPayload`
- `com.jchess.api.dto.MoveRejectedPayload`
- `com.jchess.api.dto.GameEndedPayload`
- `com.jchess.api.dto.PlayerConnectionChangedPayload`
- `com.jchess.api.dto.DrawOfferPayload`
- `com.jchess.api.dto.ErrorPayload`
- `com.jchess.websocket.GameSessionManager`
- `com.jchess.websocket.ChessWebSocketHandler`
- `com.jchess.config.WebSocketConfig`
- `com.jchess.websocket.ChessWebSocketHandlerTest`

### 2. 테스트 수행 결과 (총 71개 테스트 100% 통과)
1. `ChessWebSocketHandlerTest` (4개):
   - `GameSessionManager` 세션 등록, 브로드캐스트 및 개별 세션 전송 검증
   - `PLAY_MOVE` 수신 시 `GameService.playMove` 호출 및 `GAME_STATE_UPDATED` 이벤트 발행
   - `RESIGN` 수신 시 `GameService.resign` 호출 및 `GAME_ENDED` 이벤트 발행
   - `SYNC_STATE` 수신 시 최신 스냅샷 조회 및 회신 검증
2. REST API / GameService 테스트 12개 통과 유지
3. 규칙 엔진 단위/Perft/픽스처 테스트 55개 통과 유지

## QM / SM 검수 결과

- [x] WebSocket을 통한 실시간 수 착수 및 상태 동기화가 서버 권위형으로 안전하게 검증됨
- [x] 백엔드 (`.\gradlew check`) 및 프론트엔드 (`npm run build`) 정합성 검증 완료

판정: **최종 통과 (CP-05 완료)**

## PM 승인

- 마일스톤 완료 승인: **승인 완료**
- 다음 마일스톤: CP-06 / Phase 4 (React 19 + TypeScript 체스보드 UI, 프로모션 모달, 시계 및 실시간 WebSocket 연동 화면 구현)

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-08 | CP-05 WebSocket 실시간 대국, 세션 매니저, 브로드캐스트 핸들러 구현 및 71개 테스트 통과 |

# 04단계 검수: Game Service, JPA 저장소 및 REST API 구현 (Phase 2)

## 검수 주체

- PM: Phase 2 요구사항 충족 및 대국 라이프사이클(생성, 참가, 수 진행, 기권, 타임아웃) 검증
- QM: REST API 엔드포인트 응답 규격, 표준 에러 모델, 동시성 충돌 제어 검증
- TL: JPA 엔티티 매핑, `@Version` 낙관적 락을 통한 멱등성 및 트랜잭션 경계 확인
- SM: 서버 권위형 수 검증, 비참가자/차례 위반 차단 등 대국 공정성 및 무결성 확인

## 검수 범위

CP-04의 Game Service, 영속성 계층 및 REST API를 검수한다.

- [x] JPA Entity 및 Repository 구현 (`GameEntity`, `GameMoveEntity`, `GameRepository`, `GameMoveRepository`)
- [x] 낙관적 락(`@Version`)을 활용한 단조 증가 버전 관리 및 동시성 충돌 방지
- [x] 시간 제어(`java.time.Clock`) 추상화 및 타임아웃 자동 판정 로직
- [x] Game Service 비즈니스 로직 구현 (`createGame`, `joinGame`, `getGameSnapshot`, `playMove`, `resign`, `applyTimeout`)
- [x] 표준 에러 모델 및 예외 계층 구현 (`ErrorCode`, `ChessException`, `GlobalExceptionHandler`)
- [x] REST API 엔드포인트 구현 (`GameController`)
  - [x] `POST /api/v1/games`: 대국 생성 (201 Created)
  - [x] `POST /api/v1/games/{gameId}/join`: 대국 참가 (200 OK)
  - [x] `GET /api/v1/games/{gameId}`: 대국 상태 스냅샷 조회 (200 OK)
  - [x] `POST /api/v1/games/{gameId}/moves`: 수 착수 및 서버 권위형 검증 (200 OK)
  - [x] `POST /api/v1/games/{gameId}/resign`: 기권 처리 (200 OK)
- [x] 통합 테스트 및 동시성 검증 (`GameServiceTest`, `GameControllerTest`, `GameConcurrencyTest`)

## 수행 결과

### 1. 주요 구현 산출물
- `com.jchess.api.dto.ErrorCode`
- `com.jchess.domain.exception.*` (`ChessException`, `GameNotFoundException`, `UnauthorizedPlayerException`, `NotYourTurnException`, `IllegalMoveException`, `GameVersionConflictException`, `GameAlreadyFinishedException`)
- `com.jchess.infrastructure.persistence.GameEntity`
- `com.jchess.infrastructure.persistence.GameMoveEntity`
- `com.jchess.infrastructure.persistence.GameRepository`
- `com.jchess.infrastructure.persistence.GameMoveRepository`
- `com.jchess.config.ClockConfig`
- `com.jchess.service.GameService`
- `com.jchess.api.controller.GameController`
- `com.jchess.api.handler.GlobalExceptionHandler`

### 2. 테스트 수행 결과 (총 67개 테스트 100% 통과)
1. `GameServiceTest` (7개):
   - 대국 생성 시 `WAITING_FOR_OPPONENT` 상태 및 플레이어 배정
   - 상대방 참가 시 `ACTIVE` 상태 전이
   - 정상 수(e2e4) 진행 시 차례 전환, FEN 갱신, 기보 저장
   - 비차례 요청 시 `NotYourTurnException` 차단
   - 불법 수 요청 시 `IllegalMoveException` 차단
   - 제3자 요청 시 `UnauthorizedPlayerException` 차단
   - 기권(Resign) 시 정상 승패 및 사유(`RESIGNATION`) 기록
2. `GameControllerTest` (4개):
   - `POST /api/v1/games` 201 Created 응답 및 헤더 기반 플레이어 연동
   - 생성 -> 참가 -> 수 착수 -> 스냅샷 조회 전체 REST 라이프사이클 검증
   - 미존재 대국 404 Not Found 및 표준 `ApiErrorResponse` 응답 검증
   - 차례 위반 400 Bad Request 및 `NOT_YOUR_TURN` 에러 코드 검증
3. `GameConcurrencyTest` (1개):
   - 동일 버전에 대한 동시 착수 요청 시 1건만 정상 확정되고 다른 1건은 `GAME_VERSION_CONFLICT`로 거부되는 동시성 제어 검증
4. 도메인/규칙 엔진 단위 테스트 55개 전원 통과 유지

## QM / SM 검수 결과

- [x] 클라이언트의 임의 수/보드 조작이 서버 규칙 엔진과 JPA 트랜잭션에 의해 100% 차단됨
- [x] 동시 수 착수 레이스 컨디션 시 낙관적 락으로 단일 수만 확정됨을 멀티스레드 테스트로 증명
- [x] 백엔드 (`.\gradlew check`) 및 프론트엔드 (`npm run build`) 정합성 검증 완료

판정: **최종 통과 (CP-04 완료)**

## PM 승인

- 마일스톤 완료 승인: **승인 완료**
- 다음 마일스톤: CP-05 / Phase 3 (WebSocket 기반 실시간 대국, 핸드셰이크 인증 및 세션 동기화)

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-08 | CP-04 Game Service, JPA 저장소, REST API 및 동시성 제어 구현, 테스트 67종 통과 및 검증 완료 |

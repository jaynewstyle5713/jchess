# 02단계 검수: UX 상태 모델 및 REST/WebSocket 계약

## 검수 주체

- PM: 사용자 흐름 및 릴리스 요구사항 확인
- QM: 프로토콜 계약, 에러 모델 및 타입 정합성 확인
- TL: 백엔드/프론트엔드 DTO/Type 구조 및 실시간 동기화 설계 확인
- UX: 대국 화면 상태 전이도 및 사용자 피드백 매핑 확인

## 검수 범위

CP-02의 상태 모델, 인터페이스 명세 및 공통 타입 정의를 검수한다.

- [x] REST API DTO 및 엔드포인트 규격 정의 (`docs/api-websocket-spec.md`)
- [x] WebSocket Command / Event Envelope 규격 정의 (`docs/api-websocket-spec.md`)
- [x] 공통 에러 코드 카탈로그 및 응답 모델 정의
- [x] UX 화면 상태와 서버 이벤트 간의 매핑표 작성
- [x] Frontend TypeScript 타입 선언 (`frontend/src/types/game.ts`, `frontend/src/types/protocol.ts`)
- [x] Backend Java DTO 및 Domain Enum 스켈레톤 구현 (`com.jchess.domain.model.*`, `com.jchess.api.dto.*`)
- [x] Backend 및 Frontend 빌드 정합성 검증

## 수행 결과

### 1. 프로토콜 명세서
- 산출물: `docs/api-websocket-spec.md`
- 포함 내용:
  - 공통 에러 응답 및 에러 코드 13종 카탈로그
  - 게임 생성, 참가, 상태 조회 REST API
  - 5종 WebSocket Command (`PLAY_MOVE`, `RESIGN`, `OFFER_DRAW`, `RESPOND_DRAW`, `SYNC_STATE`)
  - 5종 WebSocket Event (`GAME_STATE_SNAPSHOT`, `GAME_STATE_UPDATED`, `MOVE_REJECTED`, `GAME_ENDED`, `PLAYER_CONNECTION_CHANGED`)
  - 8종 UX 화면 상태 매핑 규칙

### 2. 코드 타입 구현
- Backend: Java Record 및 Enum 빌드 검증 (`.\gradlew check` 통과)
- Frontend: TypeScript 엄격 모드 컴파일 검증 (`npm run build` 통과)

## QM 검수 결과

- [x] 불법 수 및 비정상 요청에 대한 명확한 에러 코드 분기 정의
- [x] 단조 증가 버전(`gameVersion`)을 통한 멱등성 및 순서 보장 설계
- [x] Frontend / Backend 간 타입 정의 불일치 없음

판정: **최종 통과 (CP-02 완료)**

## PM 승인

- 마일스톤 완료 승인: **승인 완료**
- 다음 마일스톤: CP-03 (순수 Java 기반 서버 체스 규칙 엔진 구현 - TDD)

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-08 | CP-02 REST/WebSocket 프로토콜 명세서 작성, 타입 선언 및 검증 통과 |

# 01단계 검수: 개발 환경 설치 및 프로젝트 초기화

## 검수 주체

- PM: 계획 범위와 재현 절차 확인
- QM: 설치 결과와 검증 명령 확인
- TL: Java/Node 도구 버전과 프로젝트 요구사항 확인

## 검수 범위

CP-01의 개발 환경 설치 및 스켈레톤 프로젝트 초기화 단계 중 다음 항목을 확인한다.

- Java 25 이상 JDK (Microsoft OpenJDK 25 LTS)
- Node.js LTS와 npm
- PostgreSQL 18
- Gradle Wrapper (Gradle 9.7.1)
- Spring Boot 4 Backend 초기화 및 테스트 통과
- Vite + React + TypeScript Frontend 초기화 및 빌드 통과
- 설치 및 초기화 결과가 `init_SETUP.md`에 기록되었는지 여부

## 수행 결과

### Java

- 패키지: Microsoft Build of OpenJDK 25
- 확인 버전: `25.0.4.1` LTS
- 검증 명령: `java -version`, `javac -version`
- 결과: 통과

### Node.js & npm

- 패키지: Node.js LTS
- 확인 버전: `v24.19.0`
- npm 버전: `11.17.0`
- 검증 명령: `node --version`, `npm --version`
- 결과: 통과

### PostgreSQL

- 패키지: PostgreSQL 18
- 확인 버전: `18.6`
- 서비스 상태: `postgresql-x64-18` (Running, Port: 5432)
- PATH 설정: `C:\Program Files\PostgreSQL\18\bin`
- 검증 명령: `psql --version`, `Test-NetConnection -ComputerName localhost -Port 5432`
- 결과: 통과

### Backend 스켈레톤

- 기술 스택: Java 25, Spring Boot 4.1.1, Gradle 9.7.1 Wrapper
- 검증 명령: `cd backend; .\gradlew check`
- 결과: 통과 (`BUILD SUCCESSFUL`)

### Frontend 스켈레톤

- 기술 스택: Vite 6.2.0, React 19.2.8, TypeScript 5.7.2
- 검증 명령: `cd frontend; npm run build`
- 결과: 통과 (`✓ built in 1.34s`)

## QM 검수 결과

- [x] Java 25 이상 설치 및 버전 확인
- [x] Node.js LTS 및 npm 설치/버전 확인
- [x] PostgreSQL 설치 및 `psql` 확인
- [x] Backend 빈 프로젝트 빌드/테스트
- [x] Frontend 빈 프로젝트 빌드/테스트
- [x] 모든 버전과 명령이 `init_SETUP.md`에 기록됨

판정: **최종 통과 (CP-01 완료)**

## PM 승인

- 계획 승인: 완료
- 이 단계 릴리스 승인: **승인 완료**
- 다음 마일스톤: CP-02 (UX 흐름, 상태 모델, REST/WebSocket 계약 및 에러 모델 설계)

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-08 | Java 25와 Node.js LTS 검수 통과, PostgreSQL 설치 진행 중으로 부분 통과 판정 |
| 2026-09-08 | PostgreSQL 18.6 검증 완료, Spring Boot 4 / React 19 스켈레톤 빌드 및 테스트 검증 통과로 CP-01 최종 승인 |
